# Версии зафиксированы для воспроизводимой сборки.
# Прямые зависимости проекта — в первом блоке, остальное — их транзитивные
# пакеты: удалять вручную нельзя.

# --- Прямые зависимости ---
aiogram==3.31.0          # фреймворк Telegram-бота
httpx==0.28.1            # HTTP-клиент: запросы к OpenRouter, чтение SSE-потока
sqlalchemy==2.0.36       # ORM для работы с БД
aiosqlite==0.20.0        # асинхронный драйвер SQLite для SQLAlchemy
python-dotenv==1.2.3     # чтение .env в config.py
aiofiles==25.1.0         # асинхронная работа с файлами

# --- Транзитивные зависимости ---
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
annotated-types==0.8.0
anyio==4.15.1
attrs==26.1.0
certifi==2026.7.22
dotenv==0.9.9            # ⚠️ посторонний пакет с PyPI, конфликтует с python-dotenv — кандидат на удаление
frozenlist==1.8.0
h11==0.16.0
httpcore==1.0.9
idna==3.19
magic-filter==1.0.12
multidict==6.8.0
propcache==0.5.4
pydantic==2.13.5
pydantic_core==2.46.5
typing-inspection==0.4.4
typing_extensions==4.16.0
yarl==1.25.1
